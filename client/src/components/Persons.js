import React from 'react'

const Persons = () => {
	return (
		<div>
			<h2>Persons</h2>
		</div>
	)
}

export default Persons

// {persons.map((person) => (
//     <div key={person.name}>
//         {person.name} {person.phone}
//     </div>
// ))}
